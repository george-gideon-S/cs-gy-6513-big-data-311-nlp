# minimal deps for the deployed streamlit dashboard.
# heavy training-only deps (pyspark, sentence-transformers, gensim, etc)
# live in requirements-train.txt and are only installed in colab.
#
# everything here is pure-python or has reliable wheels on streamlit cloud.
# avoid gensim/wordcloud at deploy time - they need compilation and break.

streamlit>=1.31
pandas>=2.0
# numpy upper bound removed: colab + modern streamlit cloud both ship
# numpy 2.x. forcing <2 here breaks colabs preinstalled pandas (binary
# ABI mismatch, "numpy.dtype size changed" error).
numpy>=1.24

# plotting + maps
plotly>=5.18
matplotlib>=3.7
folium>=0.15
streamlit-folium>=0.18

# pure-python ml inference (no spark needed at serve time)
scikit-learn>=1.3

# spark-compatible hash. spark MLlib's HashingTF uses MurmurHash3_x86_32
# with seed=42 internally. at deploy time the classifier needs the same
# hash function so token buckets match training-time buckets - python's
# built-in hash() is randomized per-process and degrades the classifier
# to predicting the class prior on every input.
mmh3>=4.0

# api fetches for live pulse
requests>=2.31
